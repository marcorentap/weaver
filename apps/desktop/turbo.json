{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["out/**"]
    },
    "dev": {
      "env": ["ELECTRON_RENDERER_URL"]
    }
  }
}
